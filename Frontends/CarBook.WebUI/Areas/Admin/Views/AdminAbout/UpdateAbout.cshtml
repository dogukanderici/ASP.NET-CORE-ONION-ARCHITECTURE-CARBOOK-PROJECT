@model AdminUIAboutViewModel

@{
    ViewData["Title"] = "Hakkımızda Bilgi Güncelleme";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}


<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">

                        <h4 class="card-title">Ana Sayfa About Alanı Düzenleme</h4>
                        <p class="card-subtitle mb-4">
                            Ana Sayfada Görüntülenecek olan About Alanını İçin Bilgileri Bu Ekrandan Düzenleyebilirsiniz!
                        </p>

                        <form id="UpdateAboutForm" asp-area="Admin" asp-controller="AdminAbout" asp-action="UpdateAbout" method="post">
                            @Html.HiddenFor(x => x.UpdateData.AboutID)
                            <div class="row">
                                <div class="form-group col-xl-3">
                                    <label asp-for="@Model.UpdateData.Title">Başlık</label>
                                    <input asp-for="@Model.UpdateData.Title" class="form-control mb-3" placeholder="Başlık"></input>
                                </div>
                                <div class="form-group col-xl-3">
                                    <label asp-for="@Model.UpdateData.Description">Açıklama</label>
                                    <input asp-for="@Model.UpdateData.Description" type="text" class="form-control" placeholder="Açıklama">
                                </div>
                                <div class="form-group col-xl-3">
                                    <label asp-for="@Model.UpdateData.ImageURL">Görsel URL</label>
                                    <input asp-for="@Model.UpdateData.ImageURL" class="form-control mb-3" placeholder="Görsel URL"></input>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-primary waves-effect waves-light">Kaydet</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>