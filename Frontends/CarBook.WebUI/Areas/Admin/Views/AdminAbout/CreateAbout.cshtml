@model AdminUIAboutViewModel

@{
    ViewData["Title"] = "Hakkımızda Bilgisi Ekleme";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}


<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">

                        <h4 class="card-title">Hakkımızda Alanı Ekleme</h4>
                        <p class="card-subtitle mb-4">
                            Ana Sayfada Görüntülenecek olan Hakkımızda Alanı İçin Bilgileri Bu Ekrandan Kaydedebilirsiniz!
                        </p>

                        <form id="AddNewAboutForm" asp-area="Admin" asp-controller="AdminAbout" asp-action="CreateAbout" method="post">
                            <div class="row">
                                <div class="form-group col-xl-3">
                                    <label asp-for="@Model.CreateData.Title">Başlık</label>
                                    <input asp-for="@Model.CreateData.Title" class="form-control mb-3" placeholder="Başlık"></input>
                                    @if (!ViewContext.ModelState.IsValid && ViewContext.ModelState["CreateData.Title"].Errors.Any())
                                    {
                                        <span asp-validation-for="@Model.CreateData.Title" class="alert alert-danger d-flex justify-content-center"></span>
                                    }
                                </div>
                                <div class="form-group col-xl-3">
                                    <label asp-for="@Model.CreateData.Description">Açıklama</label>
                                    <input asp-for="@Model.CreateData.Description" type="text" class="form-control" placeholder="Açıklama">
                                    @if (!ViewContext.ModelState.IsValid && ViewContext.ModelState["CreateData.Description"].Errors.Any())
                                    {
                                        <span asp-validation-for="@Model.CreateData.Description" class="alert alert-danger d-flex justify-content-center"></span>
                                    }
                                </div>
                                <div class="form-group col-xl-3">
                                    <label asp-for="@Model.CreateData.ImageURL">Görsel URL</label>
                                    <input asp-for="@Model.CreateData.ImageURL" class="form-control mb-3" placeholder="Görsel URL"></input>
                                    @if (!ViewContext.ModelState.IsValid && ViewContext.ModelState["CreateData.ImageURL"].Errors.Any())
                                    {
                                        <span asp-validation-for="@Model.CreateData.ImageURL" class="alert alert-danger d-flex justify-content-center"></span>
                                    }
                                </div>
                            </div>
                            <button type="submit" class="btn btn-primary waves-effect waves-light">Kaydet</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>