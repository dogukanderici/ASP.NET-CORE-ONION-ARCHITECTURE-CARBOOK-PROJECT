@model AdminUITestimonialViewModel

@{
    ViewData["Title"] = "Yeni Müşteri Yorumu Ekleme";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">

                        <h4 class="card-title">Yeni Müşteri Yorumu Bilgisi Ekleme</h4>
                        <p class="card-subtitle mb-4">
                            Müşteri Yorumu Bilgisi Alanı İçin Bilgileri Bu Ekrandan Kaydedebilirsiniz!
                        </p>

                        <form id="AddNewTestimonialForm" asp-area="Admin" asp-controller="AdminTestimonial" asp-action="CreateTestimonial" method="post">
                            <div class="row">
                                <div class="form-group col-xl-4">
                                    <label asp-for="@Model.CreateData.Name">Ad / Soyad</label>
                                    <input asp-for="@Model.CreateData.Name" type="text" class="form-control mb-3" placeholder="Ad / Soyad"></input>
                                </div>
                                <div class="form-group col-xl-4">
                                    <label asp-for="@Model.CreateData.Title">Unvan</label>
                                    <input asp-for="@Model.CreateData.Title" class="form-control mb-3" placeholder="Unvan"></input>
                                </div>
                                <div class="form-group col-xl-4">
                                    <label asp-for="@Model.CreateData.ImageURL">Görsel</label>
                                    <input asp-for="@Model.CreateData.ImageURL" type="text" class="form-control mb-3" placeholder="Görsel" />
                                </div>
                            </div>
                            <div class="row">
                                <div class="form-group col-xl-12">
                                    <label asp-for="@Model.CreateData.Comment">Yorum</label>
                                    <textarea asp-for="@Model.CreateData.Comment" rows="8" class="form-control mb-3" placeholder="Yorumunuzu Buraya Yazabilirsiniz..."></textarea>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-success waves-effect waves-light">
                                Kaydet
                                <i class="mdi mdi-content-save-move ml-1"></i>
                            </button>
                            <a class="btn btn-danger mi-2" href="/admin/testimonial">
                                Vazgeç
                                <i class="mdi mdi-marker-cancel ml-1"></i>
                            </a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>